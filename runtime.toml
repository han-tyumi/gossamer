# Minimum runtime versions where `gleam test` passes for gossamer's `src/`.
# Verified or updated by `./scripts/find-runtime-floor.sh`.
# Rendered into README.md badges by `./scripts/render-runtime-floors.sh`.

[floors]
nodejs = "24.0.0"
deno = "2.6.6"
bun = "1.3.13"
